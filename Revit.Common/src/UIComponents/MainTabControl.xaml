<UserControl x:Class="Eduardoos.RevitApi.MainTabControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Eduardoos.RevitApi"
      mc:Ignorable="d"
      d:DesignHeight="520"
      d:DesignWidth="320"
	>

	<TabControl
        BorderThickness="0"
        ItemsSource="{Binding Tabs}"
        SelectedItem="{Binding SelectedTab, Mode=OneWay}"
        >
		<TabControl.ItemContainerStyle>
			<Style TargetType="TabItem">
				<Setter Property="Visibility" Value="Collapsed" />
			</Style>
		</TabControl.ItemContainerStyle>

		<!-- The ContentTemplate will automatically use the DataTemplates
             defined in UserControl.Resources to render the content of the selected tab. -->

	</TabControl>
</UserControl>